interface:
  display_name: "TableProof"
  short_description: "Audit CSV and TSV joins before they corrupt research data"
  default_prompt: "Use $table-proof to audit these tables, confirm the scientific row entities and keys, and explain any unsafe join."
policy:
  allow_implicit_invocation: true
