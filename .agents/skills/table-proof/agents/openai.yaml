interface:
  display_name: "TableProof"
  short_description: "Check CSV and TSV join keys and row counts"
  default_prompt: "Use $table-proof to check these tables and explain any join-policy violations."
